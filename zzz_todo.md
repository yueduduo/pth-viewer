ol Python 环境切换 (最简单，但影响 child_process 的调用方式)。

ol 一点 缓存机制 (纯 TS 逻辑，不涉及 UI)。
    min/max等信息，下一步也加入到cache.json当中
    修改改ui渲染逻辑和渲染的json
    也要实现查看内部数据的页面，实现折叠

ol Python 脚本升级 (增加 min/max 等统计信息的计算)。

UI 重构 (引入 Toolkit，实现层级折叠算法)。

基本 ol  safetensor的支持 与 多文件关联